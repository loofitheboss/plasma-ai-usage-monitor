{
  // MCP Server Configuration — Plasma AI Monitor (no project-specific servers)
  //
  // Shared servers (github, context7, fetch, memory, sequential-thinking,
  // playwright) are defined in the primary workspace folder
  // (Veo Prompt Generator) and available here automatically.
  //
  "servers": {}
}
